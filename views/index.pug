extends layout

block content
  h1= title
  p Welcome to #{title}
  form(enctype='multipart/form-data' method='post')
    input(type='file' multiple name='file')
    button(type='submit') 提交
  hr
  ul
    for dir in dirs
      li
        a(href=base+dir)=dir
